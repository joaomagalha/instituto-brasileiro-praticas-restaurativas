# macOS
.DS_Store
._*

# CLAUDE.md local (cita caminhos internos do vault pessoal, não é pra vazar)
CLAUDE.md

# segredos — nunca versionar
.env
.env.*
*.pem
*.key

# dependências e build (caso o projeto passe a usar)
node_modules/
.vercel
dist/
